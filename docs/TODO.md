Last updated: 2026-06-22

# Выполнено

- Region System (базовая структура)
- Карта на MapLibre GL JS
- Выбор сценария и страны
- Отображение регионов и стран
- EconomyTick (с использованием регионов)
- ResearchTick (с учётом исследовательских центров и образования)
- PopulationTick (полная реализация с регионами)
- ResourceTick (с истощением и бонусами)
- MilitaryTick (полная реализация)
- DiplomacyTick (пороговая логика: затухание отношений, авто-союз/авто-соперник, сфера влияния)
- MapFeatureService + генерация начальных Map Features (столицы/города/заводы; порты — стаб, см. "Архитектура и техдолг")
- Агрегация данных от регионов к странам
- Сервисный слой (GameService, SimulationService, CountryService, RegionService, DiplomacyService, RegionEconomyService, MapFeatureService, LLMService)
- Валидация с zod
- Кастомные error types
- Рефакторинг роутов для использования сервисов (частично — см. техдолг)
- Система действий игрока (Backend)
- Управление бюджетом (Backend)
- Управление исследованиями (Backend)
- BudgetPanel компонент
- ResearchPanel компонент
- ActionPanel компонент
- Интеграция компонентов в GameView
- Тестовая инфраструктура на сервере (Vitest), тесты на тики (EconomyTick, ResourceTick, PopulationTick, MilitaryTick, ResearchTick) и сервисы (RegionEconomyService, MapFeatureService)
- TS strict mode на клиенте
- Переименование проекта в Geopolis (доки, UI, промты)
- Журнал архитектурных решений (`docs/DECISIONS.md`)

# В работе

- Дальнейшее покрытие тестами (сервисы помимо RegionEconomyService/MapFeatureService, агрегация, валидация)
- Отладка интерфейса

# Запланировано (по приоритету)

Порядок ниже — рекомендованный порядок работы, не просто список. **Изменено 2026-06-22**: подключение LLM (бывший P1) сознательно отложено — пользователь пока не планирует подключать модель к игре. Текущий фокус — критические баги и интерфейс. Карта/регионы — отдельно, не в этом списке (пользователь работает над ней сам).

## P0 — Баги, найденные в уже написанном коде

- ~~Асимметричный flip-flop союзов в `DiplomacyTick`~~ — исправлено (2026-06-22): `addAlly()` теперь требует взаимного порога `>70` с обеих сторон. См. `docs/DECISIONS.md`.
- ~~"Дублирующийся расчёт tension"~~ — проверено и снято (2026-06-22): `calculateDiplomaticTension` и `calculateBaseInfluence` оказались разными функциями, не дубликатом. Находка из первого прохода была неточной. Обе функции сейчас не вызываются ни из чего — оставлены как есть, они зарезервированы под Threat/Rivalry Response (P3).

## P1 — Интерфейс (текущий фокус)

Не трогаем карту/MapView/MapFeatureLayer — пользователь работает над ней отдельно. Остальные панели — можно.

- ~~единый API-клиент вместо хардкодных `http://localhost:3000` URL в `GameView.tsx`~~ — сделано (2026-06-22): `gameApi.ts` дополнен `updateBudget`/`startResearch`/`stopResearch`/`createAction`/`deleteAction`, добавлены proxy-записи в `vite.config.ts`. **Заодно найден и исправлен реальный баг**: `budget.ts`/`actions.ts` были смонтированы на `/budget`/`/actions`, но внутри роутов пути повторяли префикс (`/budget/budget`, `/actions/actions`) — клиент стучался в путь без дублирования и получал 404. Бюджет и действия игрока физически не работали. Исправлено и проверено живым запросом (`curl`).
- ~~`BudgetPanel`: 5 отдельных `useState`~~ — сделано (2026-06-22): один объект состояния `BudgetFormState`. **Жёсткую валидацию (блокировать сохранение при дефиците) не добавил осознанно** — дефицит, судя по `economy.budgetBalance`/`treasury`, допустимая часть модели (дефицитные расходы — нормальная механика), а не ошибка ввода; оставлен предупреждающий блок `⚠️ Дефицит бюджета`, как было. Если нужна жёсткая блокировка — отдельное решение.
- ~~`ActionPanel`: фильтровать регионы до принадлежащих игроку~~ — сделано (2026-06-22): `GameView` передаёт `playerRegions` вместо `game.regions`. Проверено в браузере на сценарии 1946/СССР — список региона корректно содержит только области СССР.
- `ResearchPanel`: список технологий хардкоден и не синхронизирован с серверными данными — упирается в непродуманный tech tree (`docs/TECH_TREE.md`), решить масштаб правки отдельно
- ~~`ScenarioSelector`: карточки на `div+onClick` → `button`~~ — сделано (2026-06-22), плюс кнопка повтора при ошибке загрузки. Проверено в браузере — выделение и стили карточек не сломались (добавлен `font: inherit; text-align: left; width: 100%` в CSS, чтобы убрать дефолтные стили `<button>`).
- accessibility: добавлены `aria-label` на `select`/кнопку удаления в `ActionPanel`, `aria-pressed` на карточках сценариев, `htmlFor`/`id` на слайдерах `BudgetPanel`, `role="alert"` на предупреждении о дефиците. Остальные непанельные элементы (мировой рейтинг, табы панелей) — не охвачено, можно продолжить отдельным заходом.
- ~~error boundary вокруг `GameView`~~ — сделано (2026-06-22): `client/src/components/ErrorBoundary.tsx`, оборачивает `<GameView>` в `App.tsx`, сброс возвращает в меню. Не проверено живым крашем в браузере (стандартный React API, код-ревью посчитал достаточным) — если будет реальный краш панели, стоит перепроверить визуально.
- ~~найдено при живой проверке: текст в `ActionPanel` слипается без отступов~~ — устранено по ходу следующей итерации интерфейса (`.action-header`/`.action-item` получили `gap`/`flex`-правила при добавлении CSS для `ActionPanel`).

### Редизайн интерфейса (2026-06-23, две итерации, см. `docs/DECISIONS.md` для деталей)

Первая итерация — структура с HOI4-референса (по явному запросу: "структуру/плотность, не цвета"):
- ~~Левая/правая колонки не должны быть активны всё время~~ — сделано: панели открываются по клику, не висят постоянно.
- ~~Верхняя панель статов (страна/ВВП/население/армия/стабильность/технологии/легитимность)~~ — сделано, `TopStatBar.tsx`.
- ~~Кликабельные показатели с переходом в нужную панель~~ — сделано для ВВП→Бюджет, Технологии→Исследования. Остальные (Население/Армия/Стабильность/Легитимность) не кликабельны — нет диаграммы (осознанно отложено, нет хранения истории по ходам) и нет отдельного экрана для них.
- ~~Территории — отдельная панель, не в общей левой колонке~~ — сделано, `TerritoriesPanel.tsx`.
- ~~Бюджет/Исследования/Мировой рейтинг — под верхнюю панель~~ — сделано через кнопки `Территории`/`Мир`/`Действия` в шапке.
- ~~Ресурсы внизу в виде иконок~~ — сделано как компактные коды-чипы (3 буквы + сокращённое число, полное название в `title`), без подключения иконочного шрифта.
- Не сделано осознанно: вкладки Армия/Дипломатия/Политика/Население/История/Статистика — нет данных/экранов под них.

Вторая итерация — окна (по запросу "все окна должны быть перетаскиваемыми и масштабируемыми, пересмотри с нуля"):
- ~~Перетаскиваемые окна~~ — сделано, `client/src/components/Window.tsx` + `client/src/hooks/useWindows.ts`.
- ~~Масштабируемые по содержимому~~ — сделано: размер по умолчанию подстраивается под контент (попутно исправило обрезание таблицы мирового рейтинга из первой итерации), плюс ручка ручного ресайза.
- ~~Полный пересмотр модели панелей~~ — сделано: вместо двух фиксированных слотов (инспектор/дропдаун) — список независимых окон, можно открыть несколько одновременно (например две страны для сравнения).
- Не сделано: окна не "прилипают" к границам экрана при ресайзе у края; закрытие по Esc; сохранение позиций окон между сессиями.

## P2 — Инженерный фундамент (снижает риск всего, что после)

- тестовая инфраструктура на клиенте (Vitest + React Testing Library) — сейчас отсутствует полностью; особенно нужна для `useWindows`/`Window.tsx` (drag/resize/z-index/дедупликация — логика без UI-рендера, удобно тестировать модульно)
- тесты для `LLMService`, `LLMResponseValidator`, `CountryService`, `RegionService`, `GameService`, `SimulationService`, route-слоя
- решить судьбу Zustand: либо внедрить (задокументирован как обязательный в `docs/AI_RULES.md`, в коде клиента не используется), либо снять требование — заодно решает prop-drilling

## P3 — Дизайн ИИ-стран (нужны решения по конкретным числам, не только код)

(без полноценного utility-AI — модель описана в `docs/AI_RULES.md`, раздел "Искусственный интеллект стран")

- бюджетные нудж-правила для ИИ-стран (сейчас `militarySpending`/`researchSpending`/... никогда не меняются после старта сценария)
- явный список стран-тиров для LLM-решений (вероятно совпадает с high-detail countries из `AGENTS.md`, требует подтверждения)
- Threat/Rivalry Response: детерминированный rubber-band против "снежного кома" игрока (расширить `calculateBaseInfluence`/tension-расчёт в `DiplomacyService`) — пороги/формулы не определены, **зависит от P0** (исправить flip-flop первым)
- кризис легитимности после войны (`PoliticsState.legitimacy/governmentSupport` → порог → гарантированный триггер "кризиса лидерства", форма кризиса — через LLM)
- посеянная при создании партии вариативность характера (`aggressiveness`, `riskTolerance`)

## P4 — Контент и крупные экономические/военные системы (большой объём, ничего не блокируют)

- мировой рынок, торговля, логистика (от неё зависят MapFeature-коннекторы — ж/д, канал, трубопровод, см. `docs/DECISIONS.md`)
- MapFeature как unit экономики: шаблоны по типу фичи (factory/mine/port) с input/output, числа считает регион — черновик в `docs/DECISIONS.md`, не финализировано; зависит от решения, кто считает добычу ресурсов (см. техдолг по `ProductionTick`)
- военная система: фронты, снабжение, производство техники
- исторические события, альтернативные события, национальные фокусы

## Отложено — LLM-цикл (раньше было P1, пользователь пока не подключает модель)

- route + UI: кнопка "скопировать промт" (`buildSimulationPrompt`) и поле для вставки ответа — сейчас `LLMService`/`LLMResponseValidator` написаны, но не подключены ни к чему (см. `docs/ARCHITECTURE.md`, "Цикл симуляции")
- расширение `LLMResponseValidator` на проверку масштаба/магнитуды последствий, не только структуры
- лог решений LLM с привязкой к ходу
- механизм "памяти страны" между вызовами LLM (краткая сводка позиции, формат и частота обновления не спроектированы)
- `LLMProvider`-абстракция (`ManualClipboardProvider`/`GigaChatProvider`/`OpenRouterProvider`/`OllamaProvider`) + тестирование GigaChat / OpenRouter (DeepSeek, Qwen) / Ollama на реальном промте с JSON-схемой действий

## Отдельно — карта/регионы (не приоритизируется здесь, ведёт пользователь)

- улучшение производительности карты, Vector Tiles (при необходимости)
- настоящая реализация `isCoastalRegion()` в `generateMapFeatures.ts` (сейчас всегда возвращает false — порты никогда не генерируются) — зависит от готовности геометрии регионов

## Сделано в рамках техдолга (2026-06-22)

- ~~`server/src/routes/player.ts` без валидации~~ — удалён: был мёртвым дубликатом `actions.ts`, клиент его не вызывал. Заодно убран из `index.ts` и `vite.config.ts` (proxy).
- ~~`ProductionTick` не подключён~~ — удалён: расчёт добычи ресурсов дублировал `ResourceTick` другой формулой (двойной счёт при наивном подключении), `taxIncome`/`industrialOutput`/`food` не имели потребителя. См. `docs/DECISIONS.md`.
- ~~`budget.ts`/`research.ts`/`actions.ts` мимо сервисов~~ — исправлено: добавлены `CountryService.updateBudget()`, `ResearchService`, `PlayerActionService`, роуты теперь только валидируют вход и вызывают сервис
- ~~`EconomyTick.ts` — неиспользуемый `resourceBonus`~~ — удалён, заодно удалена ещё одна мёртвая переменная `totalRegionGdp`
- ~~тесты для `DiplomacyService`/`DiplomacyTick`~~ — добавлены (32 теста). При написании найден баг — см. P0.